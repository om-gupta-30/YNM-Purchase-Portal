# ===========================================
# YNM Safety Purchase Portal - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values:
#   cp .env.local.example .env.local
#
# IMPORTANT: NEVER commit .env.local to version control!
# ===========================================

# -------------------------------------------
# Supabase Configuration (Required)
# -------------------------------------------
# Get these from: https://supabase.com/dashboard/project/_/settings/api
# 
# The project URL (public, safe to expose)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Service role key (KEEP SECRET - server-side only)
# This key bypasses Row Level Security - keep it secure!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# -------------------------------------------
# JWT Configuration (Required)
# -------------------------------------------
# Generate a secure 64-character random string:
#   node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
#
# IMPORTANT: Use a strong, unique secret in production!
JWT_SECRET=your-secure-random-jwt-secret-minimum-32-characters

# Token expiration time (optional, default: 7d)
# Examples: 1h, 12h, 7d, 30d
JWT_EXPIRE=7d
